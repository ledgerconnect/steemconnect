const initialState = {
  isFetching: false,
  isLoaded: false,
  errorMessage: '',
};

export default (state = initialState, action) => {
  switch(action.type) {
    default:
      return state;
  }
};
