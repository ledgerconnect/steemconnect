import React from 'react';
import { Icon } from 'antd';
import './Loading.scss';

const Loading = () => {
  return (
    <Icon
      type="loading"
      className="Loading"
    />
  );
};

export default Loading;
